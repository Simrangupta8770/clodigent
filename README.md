# Clodigent : Weather app

Weather app made with react : Displays the weather of a particular place by fetching weather from api dynamically.

Seach functionality : for searching the weather of a required place.